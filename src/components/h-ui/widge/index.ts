import HWidge from "./src/main.vue";
// import Vue from "vue";

// 为组件提供 install 安装方法，供按需引入
// HWidge.install = function (vue: typeof Vue) {
//   vue.component(HWidge.name, HWidge);
// };

// 默认导出组件
export default HWidge;
